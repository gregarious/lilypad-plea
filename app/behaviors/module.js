// declare the `pleaBehaviors` module
angular.module('pleaBehaviors', []);

// sanity test for new project structure
angular.module('pleaBehaviors').run(function() {
	console.log("pleaBehaviors: check");
});
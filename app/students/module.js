// declare the `pleaStudents` module
angular.module('pleaStudents', []);

// sanity test for new project structure
angular.module('pleaStudents').run(function() {
	console.log("pleaStudents: check");
});